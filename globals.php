<?php

/* 
 * La definición de las variables globales se realiza en este fichero
 */

/**
 * Variables de configuración con el SGBD
 */
global $DB_NOMBRE, $DB_HOST, $DB_PORT, $DB_USER, $DB_PASSWORD, $DB_CHARSET;

/**
 * Variables de la Interfaz de usuario
 */
global $UI_LANG, $UI_LANG_VARIANT;

/**
 * Parámetros del sistema
 */
global $SYSTEM_NAME, $SYSTEM_COPYRIGHT, $SYSTEM_CONNECTION, $SYSTEM_CHARSET, $SYSTEM_TIMEZONE, $SYSTEM_SHORT_TIMEFORMAT, $SYSTEM_LONG_TIMEFORMAT, $SYSTEM_DATABASE_TIMEFORMAT;
global $SYSTEM_CENTRE, $SYSTEM_DEBUGGER_MODE;

/**
 * Parámetros de configuración generales
 */

global $GENERAL_MAIL_CONTACT, $GENERAL_NUMBER_OF_EVALUATIONS, $GENERAL_MAX_PHOTO_SIZE, $GENERAL_MAIL_DOMAIN_RESTRICTION, $GENERAL_MAIL_DOMAIN_RESTRICTION_HUMAN;
global $GENERAL_LOGO, $GENERAL_EVALUATION_PASS;